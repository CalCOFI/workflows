# WS-M0 — the front door: the catalog's three indexes (D7)

**Umbrella:** `.claude/plans/2026-09-10 Measurements catalog — the environment's Species: one page per measurement, the catalog's three indexes on the front door, agent-scaled.md` § D7 (Ben's navigation scheme of 2026-09-10), Appendix C. **Spec:** the artifact https://claude.ai/code/artifact/acc4f6c0-814e-46fe-a79a-a804edebe196, section *Front door*.
**Agent:** `ws-opus-medium`. **Repo:** CalCOFI/CalCOFI.github.io in a worktree `~/Github/CalCOFI/.worktrees/CalCOFI.github.io-ws-m0`, branch `ws-m0`. **Wave 1 · ≈ 1.5 days** (the tabbed sections and the header submenus added 2026-09-10).

## Read first
- The umbrella's § Context, § D7, Appendix C; the mock's *Front door* section and its *What changed* note.
- `README.md` § The front door, § The dataset catalog, § The species catalog; `_layouts/default.html` (the header is driven by `products.yml` sections + a literal News link); `index.html` (the band, the bento, the section tabs, the Data section); `_includes/catalog_grid.html` (the realm heads); `_plugins/datasets.rb` `numbers` and `realms`; `assets/catalog.js` (the existing filter search over `/datasets/search.json`); `assets/species.js` (the elide helper, the tooltip); `scripts/check_layout.py`.
- `.claude/plans/2026-09-09 Landing follow-ups …` § D9 (the band) and § Measured S0/S3 (what the layout probe asserts today).

## You own
`_layouts/default.html` (the header nav), `_data/products.yml` (sections, tabs, `tab:` per product — no card text), `index.html`, `_includes/catalog_grid.html` (realm head only), `_includes/product_card.html` only if a tab needs a chip it lacks, `assets/door-search.js` and `assets/tabs.js` (new), `style.css` (additive), `scripts/check_layout.py`, `README.md` § The front door. Nothing under `_plugins/` except one read-only accessor if `numbers` lacks a count you need (say so in the hand-back).

## Do
1. **The header** (`_layouts/default.html`): six words from `products.yml` — DATA · APPS · ACCESS · BUILD · DOCS · NEWS — plus *calcofi.org ↗*; `products.yml` gains `sections[].kind` (`section` default, `page` for docs and news with a `url`) and `sections[].tabs[]` (`id, title, blurb`); the header is still generated from that one list. DATA, APPS and ACCESS carry a **submenu** listing their tabs with a count pill: CSS only (`:hover` / `:focus-within`, the trigger is the section link itself, Escape closes via one line of JS, `aria-haspopup` + `aria-expanded`), never a dropdown library; a phone tap on the word goes to the section (the submenu is `display:none` under 760 px). Mark the submenu as removable behind one class so open question 8 can be flipped without a rewrite.
2. **The sections** (`products.yml`): `datasets` → title DATA with tabs `datasets · species · measurements`; `explore` → title APPS with tabs `explorer · general · dataset · student`, every product carrying `tab:` (the Explorer → `explorer`; today's `across` → `general` except the Explorer; `one` → `dataset`; the nine `students` products → `student`); `access` → tabs `services` (erddap, storage, db-query) · `packages` (calcofi4r, calcofi4py); `build` keeps `workflows · calcofi4db · db-schema`; the `docs` product becomes the DOCS page link (`kind: page`, its card retired — keep the key and its `added:` so the ship's log and the three-slug contract are untouched); `news` a `kind: page` section; the `students` section is removed. Section ids keep their anchors; the analytics content group and the uptime slugs are card keys and do not change.
3. **The section bar** (`index.html` `.section-tabs`): DATA (with *16 datasets · 1,008 species · 79 measurements* from `numbers`, incl. the new `numbers.measurements_keys` computed in `datasets.rb` from `_data/release_coverage.json` `variables[realm == env]` as `count(distinct variable ∥ measurement_type)`, 79 on v2026.09.06) · APPS n · ACCESS n · BUILD n; a count the build cannot read collapses.
4. **The band**: the *species* and *measurements* `dd` wrap their number in `<a>` to `/species/` and `/measurements/` (fallbacks: the Explorer, and `…/explore/?var=temperature`, when the site has no record).
5. **The Observed tile** (was `.t-life`): eyebrow *What's observed · WoRMS- and NERC-keyed*; two rows — `1,008 species` *of 1,506 taxa* with the six life glyphs and *Browse the species →*; `79 measurements` *in 84 series* with five environment glyphs (`cat-physical cat-nutrients cat-carbonate cat-productivity cat-meteorology`) and *Browse the measurements →*; one caption; same cell, same height budget.
6. **DATA**: eyebrow `16 datasets · 1,008 species · 79 measurements · 17 holdings`; `h2` DATA; the blurb naming the three ways in; **one search** over the three indexes (`assets/door-search.js`, plain JS ≤ 200 lines: fetch `/datasets/search.json`, `/species/search.json`, `/measurements/search.json` once on focus — until WS-M3 ships, a build-time stub of the last from `release_coverage.json`; grouped results, four per group, "all n in …" links, Enter opens the first, Escape closes); then **three tabs with count pills** (`role=tablist`, arrow keys, `?tab=` in the URL, the first selected): *Datasets* = the release strip + today's filter row + grid, whose realm heads gain a door line (Biology: `1,008 species` · *1,506 taxa · 10 datasets · 1.3 M organism observations* · *Browse the species →*; Environment: `79 measurements` · *84 series · 5 datasets · 316 M values, 25 M at the release grain* · *Browse the measurements →*); *Species* = the species index's eyebrow, h1, lede and counts (from `site.data.species`), a search box that submits to `/species/?q=`, *Open the species catalog →*; *Measurements* = the same from `site.data.measurements` (or the coverage stub) submitting to `/measurements/?q=`. The category tiles are untouched.
7. **APPS**: eyebrow *17 apps*; tabs *Explorer · General · Dataset · Student* with pills; the Explorer tab renders the featured card at full width (the one `featured: true` card), the others the card grid as today with the lens key and the status / student chips; the Students section and its heading are removed.
8. **ACCESS**: tabs *Services · Packages*; **BUILD**: three cards; the blurb's "Read the docs" link stays.
9. One tab switcher (`assets/tabs.js`, ≤ 80 lines) serves DATA, APPS and ACCESS: buttons with `aria-selected`, panels `hidden`, the selected tab in `?tab=` and restored on load, the submenu links deep-link to a tab.
10. `check_layout.py`: the assertions in the umbrella's § Verification M0, with the byte-identity check narrowed to the hero, the reach tiles, the release strip and the category-tile markup, plus: each tabset's pills equal the number of cards in the panel; the header has exactly seven items at 1470; the submenus open on focus and close on Escape; no horizontal scroll at 375.
11. README § The front door: the six words, the tabs, the submenus, the search, the fallbacks; one paragraph each.

## Gates (stop and report)
- A count the build cannot read from the record or `release_coverage.json` — never type it.
- `check_layout.py` red after your change, or the Data section taller by ≥ 120 px at 1470.
- Any edit needed under `_plugins/` beyond the one accessor.

## Hand back
Branch + commits; the built `/` screenshots at 1470/375 both themes (`scripts/shots.sh` or shot-scraper); the `check_layout.py` output; the byte-identity diff result; the list of numbers the tab/band/doors read and where each comes from; one *Measured* line for the umbrella.
