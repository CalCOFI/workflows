# Integrator notes — wave 1a, 2026-09-03 (coordinator session; not an agent brief)

## Mechanics learned today
- Worktree isolation branches from the last **pushed** commit (aa2e950), not local HEAD (5bb45ca). Every agent
  was told `git merge --ff-only 5bb45ca`. Next time: push first, or put that line first in the prompt.
- 18 notebooks `devtools::load_all(here::here("../calcofi4db"))` (+ `../calcofi4r`). From a nested worktree
  `here()` = `.claude/worktrees/agent-<id>`, so the sibling resolves to `.claude/worktrees/calcofi4db`.
  **Fixed with symlinks** `.claude/worktrees/calcofi4db -> /Users/bbest/Github/CalCOFI/calcofi4db` (and
  `calcofi4r`); `.claude/worktrees/` is in `.git/info/exclude`. Consequence: a render in ANY worktree loads
  the sibling repo's *checked-out branch* (calcofi4db main = 3.28.0 today), never an agent's
  `../calcofi4db-ws-*` worktree — so agents that change calcofi4db must not render (their briefs say so);
  the integrator renders after merging, with the sibling checkout on the merged branch.
- A stray `workflows/calcofi4db/` directory exists (Feb 2026, one entry) — inspect and remove if empty.
- Fable returned HTTP 529 twice (E, B first launches); relaunched; if it recurs, wait and retry, do not
  switch model.
- A message to a completed agent RESUMES it — do not message a checkpointed agent until Ben is back online.

## Checkpoints (agent → branch @ sha → next step → answers to its open questions)
- **C** `ws-c` @ b0cd895 — DONE. sample 2,321 → 526 (310 subsamples + 216 examined tows), obs 1,456 unchanged, coverage 1984-05-17 → 2014-05-03, lon −132.25 → −120.675 (the +138.483 row is itself unsorted, so excluded anyway); Q14 filed, Q08 context updated, dataset_status rewritten, RELEASES entry. Rendered once (16:10:15) with publish_to_gcs FALSE then reverted to TRUE. **Merge notes:** (1) the render wrote the crab shards into the SHARED stage dir `~/_big/calcofi/parquet/cdfw_dungeness-crab/` — main's sidecar manifest (2,321) is now stale until ws-c merges; (2) the branch deletes 3 glightbox assets under `_output/` (single-doc render) — take `_output` from the branch or not, WS-F's full render regenerates it; (3) `obs.cruise_key` NULLs 87 → 97 and sorting-log cruise_key resolution 1639 → 1776 are drift from the ichthyo cruise reference since 08-14, documented inline.
- **A1** `ws-a1` @ 81a8400 — DONE. EDI is not uniformly CC-BY: phyto CC0-1.0 (+DOI), phyllosoma + euphausiids `custom` (+DOIs); zoodb/zooscan/farallon/cufes `custom`; dic/mesopelagic/crab normalized to CC-BY-4.0 (+DOIs dic 10.25921/3w9f-jd72, mesopelagic 10.6075/J0BZ64DH); `acknowledgement` moved for zoodb/zooscan/euphausiids. 14 `proposed` questions.csv rows: bottle Q10/Q11, ctd-cast Q28/Q29, mets Q29/Q30, ichthyo Q10–Q12, cufes Q06, zoodb Q10, zooscan Q06, pico Q06, farallon Q09, pic Q08. Index build passes (22 links OK). RELEASES entry added. **Merge note:** DG told to use ctd-cast Q30/Q31 to avoid a label collision.
- **DG** DONE: workflows `ws-dg` @ cf741ce; calcofi4db `ws-dg` @ 65e7c7e (worktree `../calcofi4db-ws-dg`; `declare_measurement_fields()` generalized to category/variable/derivation/is_canonical, 1249/1249 tests). r_* six types: derivation set, is_canonical FALSE; release gate added (0 violations); no r_* refs in explore/ctd-transects. questions: bottle Q09 answered + new Q12 (P_qual, Ben G), Q05 context; ctd-cast Q27 answered, Q09 answered (meaning only), **Q30** (orig/uncorrected, answered) + **Q31** (seafloor threshold, proposed). Flag chunk written unrun; n_flags_pending 0/0/0; cron ALIVE (nightly log OK; 0 accepted / 579 proposed flags; object unchanged because empty parquet is byte-identical). QC finding for Ben G: 0 casts. Bottle `derive_cruise_key` report fixed (INTEGER compare). Rasmus draft finished with ship names. RELEASES: three sections — **review at merge**: fold 'CTD flag bridge unrun' into the r_* entry (it does not alter release content). CRLF slip caught and fixed before commit.
- **Q** `ws-q` @ 7a1495d — DONE up to the auth gate: `scripts/sync_questions_sheets.R` (push/pull/dry-run/--execute), `scripts/test_sync_questions_sheets.R` (18 tests green), CLAUDE.md sentences, `questions_email.qmd` sheet-URL line; no RELEASES entry (process tooling — agree). Ben runs `googlesheets4::gs4_auth()` then `Rscript scripts/sync_questions_sheets.R push swfsc --execute`, then `push --execute`, commit `metadata/questions_sheets.yml`, then the pull round-trip and revert. Known: protection/validation applied only at tab creation; yml header comment stripped on first real push; pre-existing `cat(glue())` newline defect in questions_email.qmd (not fixed).
- **E** — Phase 0 DONE on calcofi4db `ws-e` @ f78a368 (Sonnet; worktree `/Users/bbest/Github/CalCOFI/calcofi4db-ws-e`); workflows `ws-e` = 5bb45ca untouched. 49/2,125 names change under D5; tie-break question for Phase 1 (worms:126175). Phase 1 launched on Fable (attempt 4) continuing in that calcofi4db worktree; workflows side on `ws-e-ph1`.
- **B** spike DONE (Sonnet): workflows `ws-b` @ 1bf94d9 — `## Measured (Phase 1 spike, Sonnet)` appended to the brief. Findings that change Phase 2: (1) 220,294 root samples: 23,403 exact `(cruise_key, site_key, order_occ)` matches, 72,969 nearest-datetime, 964 ambiguous, 122,958 none (mets by design; **calcofi_dic has `cruise_key` NULL on all 3,255 root samples** — a gap to file on the DIC ingest); (2) released `cruise` (14 cols, `cruise_uuid` 691/691) carries **no `_source_*` columns** — the umbrella's leak claim (from the 2026-08-13 Task 12 plan) is stale for v2026.08.25; drop that item; (3) `site.cruise_uuid` does not survive to the wrangling `site` table or `sample` — root cause untraced; the (a) gate must join through the ichthyo export's site table instead; (4) the integrity SQL fails today: **153,306 `sample` rows key a `cruise_key` absent from `cruise`** across 5 datasets (source-designated bottle/CTD cruises and pre-1951 PIC etc. — the FK is not declared, so nothing fails); one malformed key **`2019-07-`** (empty NODC, Bold Horizon; 2,255 rows, 5 datasets — the interim-ship `?XX?` path did not fire); 21,987 rows outside their cruise's date span, 99.97 % a span artefact (spans come from ichthyo tows only) but **7 `calcofi_ctd-cast` casts off by up to 948 days** → ctd-cast question. **Phase 2 must therefore start by completing the `cruise` reference** (rows for every source-designated cruise, `cruise_key_method` stamped, spans widened by every dataset's events or kept ichthyo-only with the gate tolerant) and fixing the empty-NODC key, before `check_cruise_key_integrity()` can be a hard gate. Design memo + Ed reply refinement still owed by a larger model (or the integrator).

## Pause 2 (16:29–16:35 CEST) — everything idle
E Phase 1 (Sonnet stand-in) checkpointed with no code: calcofi4db `ws-e` @ 604ed54 (empty commit on
f78a368), workflows `ws-e-ph1` @ 110cae2 (`_checkpoint_ws-e.md`). Resume it with "resume from your
checkpoint; the integrator notes are at /Users/bbest/Github/CalCOFI/workflows/.claude/plans_todo/
_integrator_notes_2026-09-03.md (main tree, commit after 5bb45ca) — read them by absolute path".
Still owed by a larger model when capacity returns: A0, H1 (wave 1b, after E-Ph1 merges), B design
memo + Ed reply refinement. Everything else in wave 1a is done.

## Resume 2 (Ben back, ~16:5x CEST) — four Fable launches
E-Ph1 (fresh Fable agent on calcofi4db `ws-e` + workflows `ws-e-ph1b`; the Sonnet checkpoint branch `ws-e-ph1`
is ignored), A0 (`ws-a0`, calcofi4db 3.30.0 heading; merges `ws-a1` into its branch to test the check),
H1 (`ws-h1` in calcofi4db/calcofi4r/calcofi4py/db-query; 3.31.0 heading), B memo (`ws-b-memo`, merges the
spike branch `ws-b`). Fallbacks if 529: E-Ph1 → resume the Sonnet checkpoint agent; A0/H1/B-memo → Opus, then wait.

## B memo DONE (Fable) — `ws-b-memo` @ 37eaeed (brief +416 lines: `## Decided (Phase 1 memo)` + `## Phase 2 checklist`; Ed draft 2 rewritten, 296 words)
- `site.cruise_uuid` is discarded by ichthyo's compat VIEW rebuild (ingest_swfsc_ichthyo.qmd l.1538–1558), not by a helper → the UUID check runs in `emit_core` before that block; count travels in `manifest.json` `mismatches$cruise_uuid`.
- **`2019-07-` root cause:** `create_cruise_key()` (l.264) runs before `apply_data_corrections()` (l.545) patches Bold Horizon's blank `shipnodc` to `39C2`; three guards only warned. Fix = reorder + refuse a blank NODC. **Consequence for WS-F:** the ichthyo `cruise` shard WILL change (one key), so F's hash gate must accept "identical except `2019-07-` → `2019-07-39C2`" and then re-run the datasets holding that key (bottle, pic-zooplankton, zooscan, phytoplankton — 2,255 rows); CTD holds 0 of them and stays skipped.
- 152 orphan cruise keys = 153,306 `sample` + **3,804,612 `obs` rows** (bottle 132 keys, CTD 25 reaching 2026-07 — the ichthyo export lags); every NODC resolves in `ship`. Decision: `complete_cruise_reference()` at release adds rows with `cruise_key_method = 'derived'` + `cruise_key_datasets`, ratchet 152; spans stay ichthyo-observed; gate tolerance 31 d. Also: the `_new` delta merge keys on the first column (`cruise_uuid`, NULL on a delta) so nothing ever inserts — fix before `cruise_new` is used.
- DIC: all 3,255 NULL-key roots are unmatched Niskins; the source `EXPOCODE` resolves the cruise → DIC question. The 7 CTD casts are timestamp bugs → allowlist + ctd-cast question. `station_uuid` approved: self / parent / order_occ / unique-within-24 h / NULL; expected coverage bottle 78 %, CTD 80 %, PIC 65 %, crab 14 % (+310 by parent).
- Ed draft adds one ask: the full NOAA cruise-table export (CruiseId, ship, month, dates) so the 152 derived cruises get UUIDs. Questions to file at Phase 2 (labels after A1/DG land): dic Q07, ctd-cast Q32, ichthyo Q13 + Q14. `docs/db.qmd` l.35/l.46 corrections handed to WS-H2.

## A0 DONE (Fable) — workflows `ws-a0` @ 83b359a (**contains merges of ws-a1 @ 81a8400 and ws-c @ b0cd895** — merging ws-a0 brings A1 + C to main; one crab `license` conflict resolved keeping C's comments + A1's id); calcofi4db `ws-a0` @ d41baf5 (from main 8c39a9f; `# calcofi4db 3.30.0` NEWS heading; `curl` → Suggests; 1417/1417 tests)
- `check_dataset_citation()` grain = one row per (dataset, finding) with `level` ok|error|warn, `exempt`, `question`, `field`; findings add `missing_license`; exemption is field-aware (`related_field` empty covers all). Merged YAML: 0 blocking · 4 ok · 14 exempt · 2 drift warnings (dic abbreviates NCEI authors; mesopelagic vs DataCite APA) — never auto-adopted. Filed **mets Q31** (no publication year on calcofi.org; access-year form, proposed to Rasmus).
- New `dataset` columns: `doi`, `license_url`, `acknowledgement`, `contact` (after `pi_names`), `source_accessed` (DATE) + `source_accessed_method` at release. `metadata.json` datasets[] + `citation_others` array. `catalog.json`: `citation`, `concept_doi`, `doi` when minted; `versions.json` records carry `doi`.
- **`source_accessed` (sidecar_commit) = 2026-08-25 for 15 datasets, 2026-09-03 for the crab — "last run", not the download date**; honest but coarse; `stamp_source_access()` exists for future ingest runs.
- PASTA's listing/search answer 403 to public access → newest EDI revision found by probing the cite service rev 1..n. Zenodo legacy `.zenodo.json` has no ROR field; `version` omitted (Zenodo fills from the tag); PIs = DataCollector, Ben/Betty = DataCurator. `CITATION.cff` carries the concept DOI. **WS-F runs `Rscript scripts/build_citation_files.R <version> <date>` before tagging.**
- `publish_release_notes()` gained `prefix`, `zenodo`, `release_policy`, `fetch`; on a Zenodo hit it rewrites local + published `catalog.json` (`doi`, `citation`) and rebuilds `versions.json` keeping `consolidated` from `release_policy.yml`. GCS re-upload branch NOT exercised.
- `release_database.qmd` edits confined to `dataset_coverage`, the catalog write (`add_release_citation`), the `release_notes` call. `build_workflows_index.R` needs calcofi4db ≥ 3.30.0 installed (or `CALCOFI4DB_DIR=<worktree>`). New skill `.claude/skills/attribution/`; CLAUDE.md section + two registry rows. 7 `citation_authority.json` caches committed. `_output/_data/workflows.yml` restored, not committed.

## H1 DONE (Fable) — workflows `ws-h1` @ 2b21ad8; calcofi4db `ws-h1` @ d80abcc (3.31.0 heading); calcofi4r `ws-h1` @ dbf1699 (1.17.0 heading — A2 shares that version); calcofi4py `ws-h1` @ 1d8a5a1 (CHANGELOG 0.6.0; `__version__` still 0.5.0 → 1 test fails by rule until the integrator bumps pyproject/`__init__`/README quotes); db-query `ws-h1` @ e309220
- `build_obs_slim()` + `sample_key`/`measurement_prec`/`hex_id`; `obs_view_sql()` tokens, `OBS_VIEW_COLUMNS` (18, obs TABLE order: `dataset_key` third — the hive-list read puts it last, 16 mismatches otherwise), `release_view_tables()`, `check_obs_pair_parity()` (per realm×dataset signature hash). `build_release_catalog(views = release_views())` registry; catalog `views.obs` + `obs` `deprecated/replaced_by/removed_in: "next"`; `metadata/release_tables.csv`, `release_columns.csv`, `relationships_cross.csv` updated; `obs_bio`/`obs_env` core, `sample_root` supplemental.
- Resolvers: R `cc_catalog_views()/cc_view_tables()/cc_view_sql()`, `cc_get_db()` serves `obs` via the view when the pair loads; py `catalog_views()/view_tables()/view_sql()`; JS `catalogViews()/…/readParquetForCatalog()`; fixtures `catalog_canonical.json` + new `catalog_view_only.json` byte-identical in three repos. `test_release.qmd`: `tbl_sql()` expands views; 10 obs rows ×3 ways (32 rows) + view-count + column-order rows.
- Measured on staging v2026.08.28: parity OK for 15 groups; view = 26,261,931; `obs_bio` 21.8 → 25.6 MB, `obs_env` 286.7 → 317.2 MB; **depth fallback fills 482,250 ichthyo rows (100 %) and nothing else** (euphausiids/zoodb/crab/dic/pico NULL depths have no span on `sample` either — ichthyo Q08 territory).
- Not verified: `_queries` suite over GCS through the view (needs the staging run); pre-freeze notes lack the deprecation flag; `libs/publish_netcdf.R`, `scripts/render_release_views.R`, ERDDAP deploy still read physical `obs` (must migrate before the objects drop — add to H3/A4/F notes). **Install order at integration: calcofi4db, then calcofi4r** (`test_release.qmd` calls `calcofi4r::cc_view_sql()`).

## E-Ph1 DONE (Fable) — calcofi4db `ws-e` @ c36d60a (1,378 tests, `document()` run; `# calcofi4db 3.29.0`); workflows `ws-e-ph1b` @ a013108 (`metadata/taxon_group.csv` new; `taxon_common.csv` 44 rows tagged manual; `taxon_lineage.csv` +1,026 cache rows; ichthyo Q13; RELEASES section first)
- Gate: farallon 156/156 `dataset_taxon` key-for-key, 127/127 `taxon` on eight fields; `marine_mammals` 31 vs 33 (two sea turtles, class NA, leave under the class rule — correct); `check_dataset_taxon()` 0 findings; 113/113 itis: taxa are Aves, 0 worms: Aves.
- Decisions made (review): tie-break = code whose `ds_scientific_name` equals `taxon.scientific_name`, then `ds_taxon_key` → 50 (not 49) names change; class rules scope = vocabulary taxa (not ancestors); override rows for absent datasets skipped per ingest, `check_taxon_registries()` is the release-time check (**not yet wired into release_database.qmd** — Phase 3); a lone TSN on non-Aves keys local; `ds_source_json` filled for arm datasets too; positional `build_taxon_group(con, mt, over)` falls back to the CSV with a lifecycle warning; `cc_data_dir()` not built.
- **FOUND, Ben's decision before Phase 3:** phytoplankton releases only 22 `taxon_key`s for 393 codes (source has 294 distinct AphiaIDs) because six `taxa`-matched `taxon_override.csv` rows replace species ids with functional-group ids — fix the six registry rows, or an "override never replaces an id the source supplied" rule; either changes ~290 released keys. Also: rank-4 common names publish "other" ×8 and "undefined (…Q05)" ×9 as `common_name`.
- Phase 2 (farallon, PR #77): the 37 override rows switch `match_column` to `ds_taxa_code`; the 28 unidentified fallbacks become override rows.

## Integration order (E-Ph1 landed 2026-09-03 ~18:00 CEST)
calcofi4db: main ← ws-e (3.29.0) ← ws-a0 (3.30.0) ← ws-h1 (3.31.0) ← ws-dg (fold into 3.31.0 or 3.31.1); bump DESCRIPTION/NEWS once per step; `devtools::test()` after each; install. calcofi4r: ← ws-h1 (1.17.0; A2 adds `cc_cite()` to the same version). calcofi4py: ← ws-h1 (0.6.0 bump). db-query: ← ws-h1. workflows: main ← ws-a0 (carries a1 + c) ← ws-dg ← ws-q ← ws-b (spike) ← ws-b-memo ← ws-h1 ← ws-e-ph1b; resolve RELEASES.md/CLAUDE.md heading order; assign question labels (mets Q31 A0; ctd-cast Q30/Q31 DG; B's future dic Q07 / ctd-cast Q32 / ichthyo Q13–14). Then `_output` untouched until WS-F.

## Wave 1 INTEGRATED (Fable integrator, ~18:30 CEST)
- calcofi4db main 8c39a9f → **a3b590f = 3.31.0**, installed (857f924 ws-e → 3.29.0 · 36a079c ws-a0 → 3.30.0 · beff000 ws-h1 · 4f508e9 ws-dg · a3b590f bump); tests 1,624 pass / 2 warn (E's finding messages) / 0 fail. calcofi4r main **6475032 = 1.17.0**, installed (489 pass, 1 PG skip). calcofi4py **c6941bb = 0.6.0**; `pip install -e .` re-run by the coordinator → dist 0.6.0, docs tests 6/6. db-query main = e309220 (15 pass).
- workflows main 067e9e0 → **b26abb3** (7231125 ws-a0[a1+c] · 0c2a715 ws-dg · 5f155d9 ws-q · 74864b2 ws-b · 989916e ws-b-memo · c1f382a ws-h1 · b26abb3 ws-e-ph1b). Conflicts: RELEASES.md ×3 (order E → A0 → A1 → H1 → pre-existing five → C → DG's three), bottle/ctd-cast/ichthyo questions.csv (row-wise; bottle 12, ctd-cast 31, ichthyo 13 rows; all 17 registries unique ids/labels). Index build 0 blocking (4 ok / 14 exempt / 2 drift). `_output/` untouched (index regen reverted). Left: DG's three RELEASES sections verbatim (editorial fold is Ben's call); `check_taxon_registries()` not wired into release_database.qmd (E Ph3); `publish_netcdf.R` / `render_release_views.R` / ERDDAP deploy still read physical `obs` (H3 migrates).
- **New worktrees for wave 2 must `git merge --ff-only b26abb3`** (pushed state is still aa2e950).

## Wave 2 LAUNCHED (~18:35 CEST) — six agents, worktrees, branches
A2 `ws-a2` (Sonnet; calcofi4r/py worktrees `-ws-a2`; bullets under 1.17.0 / 0.6.0) · A3 `ws-a3` (Opus; `explore-ws-a3`) · H2 `ws-h2` (Opus; calcofi4db `-ws-h2` 3.32.0 heading; `docs-ws-h2`) · H3 `ws-h3` (Sonnet; erddap grain from obs_bio + publish_netcdf/render_release_views migration) · B2 `ws-b2` (Sonnet; calcofi4db `-ws-b2` 3.32.0 heading; ichthyo reorder, source_uuid, cruise_reference + key_integrity chunks) · E-Ph2 `ws-e-ph2` (Fable; from `origin/ingest-farallon-erddap` + merge b26abb3; renders farallon once with GCS off). Waiting on Ben: the phytoplankton override collapse (E Ph3 blocked). Wave 3 after these: A4, E-Ph3 (needs the decision), then F. The `.claude/agents/ws-*` definitions are now registered in this session (effort knob available) — use them for wave 3.

## H3 DONE (Sonnet) — workflows `ws-h3` @ 5673c6e; docs `ws-h3` @ e9f0bba (`docs-ws-h3` worktree; scoped ERDDAP paragraph — merges under H2's rewrite)
- `sql_obs_pair()` reads `obs_bio`/`obs_env` through `cc_release_sources()`; `obs-pair` chunk sets `HAS_OBS_PAIR` from the catalog and falls back to deprecated `obs` with a `cat()` (verified: v2026.08.25 falls back). Adds tow_type, std_haul_factor, prop_sorted, volume_sampled_m3, density_per_10m2, density_per_1000m3, effort_class, units, qual_ok. `.erddap_datavar_xml()` + `flag_values`/`flag_meanings` (measurement_qual.csv) + `sdn_parameter_urn` (from `nerc_p01`, inert until H2). Tested as standalone Rscripts on a locally rebuilt pair (staging core + current `build_obs_slim()`): 15 datasets one realm each, XML parses, `density_per_10m2` present for ichthyo.
- **Not migrated (not mechanical):** `publish_to-netcdf.qmd`'s `obs` reads — `obs_env` is partitioned by `measurement_type`, not `dataset_key`, so its one-partition-per-dataset strategy (~800 lines) breaks. `libs/publish_netcdf.R` and `scripts/render_release_views.R` have no literal `obs` reads of their own. **Must be solved before the release that drops the `obs` objects** — add to the H1 deprecation window and the F/next-release notes; follow-on in the WS-H plan file (`## H3 DONE`).

## A2 DONE (Sonnet) — calcofi4r `ws-a2` @ 6e8ad16 (`R/cite.R`, vignette, 3 fixtures, NEWS under 1.17.0, `curl` Suggests; 513 tests); calcofi4py `ws-a2` @ e7dceac (`cite.py`, `__citation__`, `docs/citing.md`, CHANGELOG under 0.6.0; 42 pass / 3 skip); workflows `ws-a2` @ 39ff8f4 (hand-back notes only). `cc_cite(x = NULL, version = "latest", format = text|bibtex|csl, con = NULL, resolve = FALSE)` in both; fixtures byte-identical (cmp). `citation_others` / `contact` unused by cc_cite (future). No RELEASES entry (package-only). A4 may start (wave 3).

## E-Ph2 DONE (Fable) — workflows `ws-e-ph2` (from `origin/ingest-farallon-erddap` b140e02 + merge of main): c53e8f0 merge · 6cc7019 registries (37 override rows → `ds_taxa_code`, 38 new rows, `species_itis_datazoo.csv`) · dbffd55 notebook (download-first, DataZoo join, `append_dataset_taxon()`, audit, `check_dataset_taxon()`, Q10 + Q11) · f5cd487 sidecars + caches (xref +11, lineage +129) · 38e573f RELEASES + plan Measured. **Ben pushes:** `git push origin ws-e-ph2:ingest-farallon-erddap` (fast-forward from b140e02); Betty reviews (six review notes in the hand-back).
- Gate: `taxon_key` 153/154 identical (MEGU `itis:176832` → `itis:1192602`, documented D3); 126/126 taxon rows; seabirds 94 → 99; **obs NULL taxon_key 1,177 → 727 on shared rows (450 gain a key, 0 lose)** — accept as D3's intended effect; obs 66,344 → 69,661 (762 NULL = the 49 excluded codes). `check_dataset_taxon()` 0 findings; audit 96 agree / 68 differ all explained.
- Upstream: **ERDDAP has no 2021 observations** (DataZoo had 625; Q11 high); `CODO`/`XCMU` names changed (Q10c); `CAC2022_8` unresolvable; `CAC2019_7` → the malformed `2019-07-` key (B2 renames). *Larus brachyrhynchus* has no WoRMS record (ITIS-only key, `worms_id` NULL).
- **Package gap for Phase 3:** `.apply_xref()` branch (c) fills only `worms_id` for a name-resolved row, so a bird without a source id can never key `itis:` even when WoRMS links the TSN — GUMU/MABO/NABO needed override rows; SCMU/TOSP/CHSP have no WoRMS link regardless. `itis:1192602` common name (MEGU "Mew Gull" vs SBIG "Short-billed gull") may want a manual `taxon_common.csv` row.
- Render facts: **Quarto treats `.claude/worktrees/` as hidden → worktree renders are standalone (0 project inputs, no brand includes)** — HTML deleted, `_output/` untouched; the shared stage dir `~/_big/calcofi/parquet/farallon_bird-mammal/` now holds the ERDDAP build (main's sidecar stale until merge, like crab). `Validation: FAILED` from `validate_for_release()` is pre-existing (nulls check treats every *_key/*_id as required, strict = FALSE).

## H2 DONE (Opus) — workflows `ws-h2` @ 07ad3dd (registries 4c78c14); calcofi4db `ws-h2` @ 5c2a369 (`declarable_measurement_fields()` + `nerc_p01`/`units_nerc_p06`, `nerc_uri_prefixes()` validation; `# calcofi4db 3.32.0` heading; 1,635 tests); docs `ws-h2` @ af0d65d (`db.qmd` rewritten, 100 internal links 0 broken)
- 115/200 measurement types carry P01, 174/200 P06; `metadata/life_stage.csv` 23 stages / 10 S11; `metadata/gear.csv` 11 codes / 4 L22; `field_dictionary.dwc_term` 12/57. 29 of the 85 unmapped types are taxon-bearing (an id would be wrong). `scripts/declare_measurement_vocab.R` idempotent (needs `CALCOFI4DB_DIR=` until ws-h2 merges). `publish_ichthyo_to-obis.qmd` reads `measurementTypeID` from the registry (not rendered). Follow-on stub written.
- Findings: **ammonia vs ammonium** — `r_ammonium`/`btl_ammonium` map to NH4+ (AMONZZXX), QC'd `ammonia` left empty because its source says NH3; one source label is wrong (bottle Q05 territory). **`larva` and `larvae` both ship** (ichthyo 379,962 / euphausiids 145) — euphausiid vocabulary normalization gap. `libs/build_field_dictionary.R` had drifted 4 rows behind its CSV (would have deleted `seafloor_depth_m`, `date_min`, `date_max`, `cruise_key_method`) — fixed.
- For H3 at merge: derive `sdn_parameter_urn` from the URI (`SDN:P01::<code>`), no new column. RELEASES: three sections appended at the END of `# Unreleased`. **Merge order note:** calcofi4db `ws-h2` and `ws-b2` both carry `# calcofi4db 3.32.0` headings → fold into one 3.32.0.

## A3 DONE (Opus) — explore `ws-a3` @ a665251 (`explore-ws-a3` worktree); workflows `ws-a3` @ c5eeb8b (CLAUDE.md only)
- All eight items: agreement button (not a gate; `?tour=off` shows nothing), Sources line (pooled datasets listed), footer third line (`FOOTER_PX` 34 → 46), Cite this data (text/BibTeX) + `dataset_key` column on every panel CSV, Sources modal (`?modal=sources`, ❞ header button, front door https://calcofi.io/), Register a product (feedback kind, label `derived-product` — **Apps Script must read `label` from the payload**, not verified live), averaging copy fixed in three files, verify.mjs +17 states (138 states green at two viewports; fixed a pre-existing `u2_prewarm` assertion). New `src/cite.ts` shared by `bundle.ts`. Degrades without every A0 column (table in README).
- No RELEASES entry (consumer change) — agree. Deploy after F flips `VITE_RELEASE_PREFIX` (untouched). Measured: sardine default pools 1 dataset, temperature pools 2 (bottle + CTD).

## B2 DONE (Sonnet) — calcofi4db `ws-b2` @ 21a5e1e (on a3b590f; `# calcofi4db 3.32.0` heading — fold with ws-h2's 3.32.0); workflows `ws-b2` @ 9f3e1bd
- calcofi4db: `append_sample()` 17th col `source_uuid`; `create_cruise_key()`/`resolve_cruise_key()` refuse a blank NODC; new `R/keys.R` — `complete_cruise_reference()`, `check_cruise_key_integrity()` (10 checks/ratchets), `match_station_occupation()`; `{table}_new` delta merge dedups on the declared PK (was ordinal-first). **Tests reported 394/394 in 47 files — far below main's 1,624: run the FULL suite after merging** (likely a filtered run).
- workflows: ichthyo notebook reordered (corrections before key minting), `source_uuid` in the 3 arms, UUID-mismatch assertion before the compat VIEW; `release_database.qmd` + `complete_cruise_reference()` call, new `cruise_key_integrity` chunk (replaces the warn-only regexp), `match_station_occupation()` in `depth_coverage`; `test_release.qmd` contract row; `field_dictionary.csv`, `relationships_cross.csv`; questions dic Q07, ctd-cast Q32, **ichthyo Q14/Q15** (Q13 = E's); CLAUDE.md, RELEASES.md.
- Gate SQL on v2026.08.25 reproduces the spike (2,255 / 1 key; 153,306 / 152 keys). **New:** `2019-07-39C2` already exists with 1,149 rows (CTD 140, METS 1,009 mint it from archive names) → after the fix the two spellings converge (3,404 sample / 148,026 obs rows). Gate cannot pass on the frozen release; passes after the re-run.
- **WS-F additions:** hash gate treats "identical except `2019-07-` → `2019-07-39C2`" as NOT safe to skip; re-run bottle, ichthyo, pic-zooplankton, zooscan, phytoplankton; CTD stays skipped (0 rows, keys from archive names — verify via `content_hash`); optional mets re-run resolves its 2015-10/11 32OC overlap.

## A4 DONE (Sonnet, registered agent) — db-schema `ws-a4` @ 154ba2e; ctd-transects `ws-a4` @ 68ffdd8; docs `ws-a4` @ 08892b6 (new `cite.qmd`, `_quarto.yml`, `portals.qmd`); workflows `ws-a4` = no changes
- db-schema: license chip links (custom → `license_url`, SPDX → spdx.org), DOI/contact/acknowledgement on dataset cards, "How to cite this release" card from `catalog.citation`, `deprecated → replaced_by` chip on Tables; renders unchanged on a legacy catalog; 11/11 tests. ctd-transects: `scripts/fetch_citation.py` folds the dataset + release citation into `version.json` (`refresh.yml`, jq merge); About + footer hidden until a release carries it; 6 new tests. docs `cite.qmd` rendered against v2026.08.25 (17 rows, computed citation, "predates the contract" notice).
- **BUG found in A2:** `cc_cite()` hardcodes the new columns → DuckDB binder error on every pre-contract release (v2026.08.25 `dataset` has 18 columns). Integrator #2 told to fix in R + py (select present columns, NA the rest, legacy-shaped test) BEFORE installing calcofi4r. Note for Betty (db-viz-station `build_datasets.sql` + Pooh's heading) in the hand-back. `db.qmd` one-line link handed to the integrator.

## WS-Q LIVE (2026-09-04, service account) — six provider sheets in Shared Drive folder `questions` (1Tryjfd76TNU4nVPPSQiyQCy03uo0hlx6, under "CalCOFI Data Folder")
calcofi 1uW9GLogdD2K6NiQGS_xJiIPFUPUvifWesrLlK5UUK7g · swfsc 1kQM6aw3yiT1AZAmFfgCp2Ou9u-UckR69Tien_4Mk1qE · sio 1Te26TbEr_Snz4A2818VcEi5tMj2Q9vNuUxvhKWBkUo0 · cce-lter 1vExO9H6g9fTJ-kisz5LJ0eu02V8t8nrXwi1IVGoQHd8 · cdfw 1cOEo_ylXNPgQDp3wCMz7DHo_5v1jz65nyhXAN9KK_EE · farallon 1szPLnPuQtaskPpwB1dkX7kGqs4hj6I2g2cE_kGq6ZbU; sccoos none.
- Root cause of the 403: a service account has no My Drive quota, so `gs4_create()` fails; sheets are created via the Drive API inside the Shared Drive folder (owned by the drive). The SA can add/trash but not delete there (a probe file `_probe_calcofi-admin_delete-me` sits in the Shared Drive trash). Files inherit the drive's membership (Ed Weber, Rasmus, Erin, Mark Gold, Noelle Bowlin… already have access); per-file shares are silent (`sendNotificationEmail = FALSE`).
- Round trip proven on swfsc (cell edit → pull dry-run diff + auto-flip → `--execute` → clear → `--execute` → CSV identical). Polish left: an auto-flipped row gets no `answered_date`. `asked_date` stamped today on every open/proposed row (the push, not the e-mail, is "asked").
- Run non-interactively with `QS_GOOGLE_SA_JSON="~/My Drive/private/2026-06-07 ucsd-sio-calcofi_36230b2795e6_calcofi-admin-sa.json"`.

## ws-e-ph2 MERGED to main (Ben, 2026-09-04) — c5b9ac9 (one conflict: farallon questions.csv, kept main's asked_date stamps + the branch's Q10/Q11); 99b0500 farallon sheet re-pushed (11 rows). Betty's review is on the PR after `git push origin ws-e-ph2:ingest-farallon-erddap` (Ben). The stage dir already holds E-Ph2's ERDDAP farallon build.

## E-Ph3a DONE (Fable, 2026-09-04) — calcofi4db `ws-e3a` @ d24bd32 (from 3.32.0; `# calcofi4db 3.33.0`; 1,762 tests); workflows `ws-e3a` @ 6499080 (release_database.qmd wiring, RELEASES, CLAUDE.md, taxon-reference skill, plan, `taxon_override.csv` +1 (code 600), `taxon_xref.csv` +510, `taxon_lineage.csv` +3,035)
- Override rule in `.apply_overrides()` (non-code match only where the source supplied no id; code match wins, runs last); `report_taxon_overrides()` exported; `_taxon_override_report` staged. **Phyto gate: 393 codes 22 → 309 keys; 302 codes change (147/117/35/3 off the four class keys), 70 stay class-level (no source id), 10 local; taxon shard 50 → 542 rows; taxon_group phyto 24 → 311; 124,586 of 159,804 phyto obs change key.** Found: code 600 "Actinocyclus, uncertain species" resolved in `metadata/calcofi/phytoplankton/taxon_worms.csv` to AphiaID 196347 = the NUDIBRANCH genus; diatom is 148944 → code-matched override row added; **3b fixes `taxon_worms.csv` and drops the row.**
- `apply_taxon_common(group_rules=)`: rank 4 refuses group labels + labels of local keys → other 175 → 151, empty 930 → 954; D5 changes 50 → 26. `.apply_xref()` (c) takes the linked TSN → GUMU/MABO/NABO key `itis:` without rows; CHSP/SCMU/TOSP keep theirs. Farallon 164/164 unchanged. `check_taxon_registries()` + `report_taxon_overrides()` wired into `taxon_authority_coverage`; new `taxon_override_report` chunk.
- 3b list: drop override rows GUMU/MABO/NABO/CSLI (+ code 600 after the csv fix); migrate ichthyo, phyto, zoodb, zooscan, euphausiids, mesopelagic to `append_dataset_taxon()`; phyto/crab positional `build_taxon_group()`; delete `.override_match_alias` with the arms → phyto `taxa` rows become `ds_common_name` rows; calcofi4db 4.0.0.
- Worktree base carried Ben's `aa43cde` (provider.csv SWFSC → NOAA SWFSC), not on local main → kept on branch `keep-provider-swfsc-aa43cde`; check origin.

## E-Ph3a + 3b INTEGRATED (2026-09-04) — calcofi4db main 3.33.0 (a36674c) → **4.0.0 (419a674)**: 3b merged + the upsert fix; 1,774 tests; installed. workflows main: 087b2d6 (3a) → merge of `ws-e3b` @ 85aca44
- 3b gate: ichthyo 1,167/1,167, zoodb 33/33, zooscan 23/23, euphausiids 37/37 (+1 unreferenced `euphausiidae` vocabulary row leaves), mesopelagic 90/90; phyto 302/393 change by design (22 → 309 keys; taxon 50 → 542; taxon_group 24 → 311; 124,586 obs re-keyed); code 600 fixed in `taxon_worms.csv`. Six shards re-rendered into `~/_big/calcofi/parquet/` — WS-F re-renders them anyway. `_output/` untouched. Farallon not re-rendered (3a's 164/164 stands).
- **BLOCKER FIXED in 4.0.0:** `upsert_measurement_types()` keeps the registry-owned fields (`declarable_measurement_fields()`) across an ingest re-run — registry value wins even over an explicit literal; new types take the literal (test added). Without it every re-run ingest blanked H2's ids and DG's `is_canonical = FALSE`.
- 3b kept the `build_dataset_taxon()` deprecated alias; the measurement-path notebooks call `resolve_dataset_taxon()`.

## Resume message (send to each when Ben is back)
"Resume from your checkpoint (`.claude/plans_todo/_checkpoint_ws-<id>.md` on your branch). Two things
changed: (1) `.claude/worktrees/calcofi4db` and `calcofi4r` are now symlinks to the sibling repos, so
`devtools::load_all(here::here("../calcofi4db"))` works from your worktree; (2) <answers above>.
Continue the brief; hand back per the rules."

## Wave 2 INTEGRATED (Fable integrator #2, ~21:35 CEST) — nothing pushed; ws-e-ph2 NOT merged (Ben's); ws-a4 workflows = no changes
- **Harness deviation (workflows only):** a worktree-isolated agent may not run git against the shared `workflows` checkout (`cd`/`-C`/loops all refused), so the workflows merges live on this worktree's branch **`worktree-agent-aca2c577cb51c5799`** (ff'd from b26abb3): 91d58a2 ws-a2 · 604743b ws-h3 · 77801a1 ws-h2 · 0dd7ebe ws-b2 · 4d4f932 ws-a3 · then this notes commit. **Coordinator: in the main tree `git checkout -- .claude/plans_todo/_integrator_notes_2026-09-03.md` (its uncommitted 39 lines are inside this commit verbatim — diff first) then `git merge --ff-only worktree-agent-aca2c577cb51c5799`.** Sibling repos were merged on their real `main`s (git `-C` to a sibling is allowed). Next integrator: launch without worktree isolation.
- **calcofi4db** main a3b590f → **84b6e5d = 3.32.0**, installed (b689b8d ws-h2 · d9b1ead ws-b2 · 84b6e5d bump). Conflict: `NEWS.md` — both branches' `# calcofi4db 3.32.0` folded into one heading, H2's registry-ids section first, B2's keys section second. `document()` changed nothing. **Full `devtools::test()`: 1,711 pass / 0 fail / 0 error / 2 warnings (E's `check_dataset_taxon()` finding messages, as in wave 1) in 47 files** — main 1,624 + H2's 11 + B2's 76; B2's "394" was a filtered run, nothing is missing.
- **calcofi4r** main 6475032 → **3d1c23b = 1.17.0** (no bump), installed (0efe890 ws-a2 · 3d1c23b the A4-found fix): `.cc_cite_rows()` now selects the columns `DBI::dbListFields(con, "dataset")` reports (`.CC_CITE_DATASET_COLS`), NA for an absent one, clear error when `dataset_key` is missing; new `tests/testthat/test-cite-legacy.R` builds v2026.08.25's exact 18-column `dataset` (read from the published parquet: `dataset_key, provider, dataset, dataset_name, dataset_name_short, category, color, description, citation_main, citation_others, link_calcofi_org, link_data_source, link_others, tables, coverage_temporal, coverage_spatial, license, pi_names`) and asserts text/bibtex/csl each carry the release citation + 3 entries with License lines and no DOI/URL/Acknowledgement; NEWS bullet under 1.17.0's `cc_cite()` section. Tests 513 → **533 pass / 0 fail / 1 PG skip (12 files)**; `exists("cc_cite", asNamespace("calcofi4r"))` TRUE.
- **calcofi4py** main c6941bb → **20958d6 = 0.6.0** (b53d0e3 ws-a2 · 20958d6 mirror fix: `_DATASET_COLS` + `DESCRIBE dataset`, `KeyError` without `dataset_key`; `test_pre_contract_dataset_table_cites_without_doi_license_url_acknowledgement`; CHANGELOG bullet under 0.6.0). `.venv` editable install points at the checkout (`__version__` 0.6.0); pytest 42 → **43 pass / 3 skip**; the three `cite_*` fixtures `cmp` byte-identical R↔py (3/3).
- **explore** main 95c11fe → **59d426a** (ws-a3); `npm run build` (tsc --noEmit + vite) clean, 2.43 s; `dist/` ignored; not pushed (F flips the prefix first).
- **docs** checkout was on branch `test` → switched to `main` (fe50c6a; **1 behind origin/main = fd493ad "Add test2.txt"**). main → **3a21eb3**: 4829059 ws-h2 · 6e36414 ws-h3 (git auto-placed H3's ERDDAP paragraph under the DwC "Three caveats"; moved to the end of `### Publishing to portals` in 9013982) · 28699d4 ws-a4 · 3a21eb3 the `dataset.csv` row now says "(see [Cite This Data](cite.qmd) for how to cite them)". **Junk on main:** ws-h2/ws-h3/ws-a4 were all branched from `test` (24a11ff, apples/oranges), so `test.txt` + `test.R` are now on docs main — delete them (origin/main already carries `test2.txt` from the same experiments). `quarto render cite.qmd --to html` + `db.qmd --to html` → `_book/cite.html`, `_book/db.html` (ignored). **Trap:** a single-file render of this book project deleted four tracked root files (`apps.html`, `index.html`, `maps.html`, `reports.html` — restored with `git checkout --`) and left `index.aux/log/tex/toc` (removed); the working tree is clean.
- **db-schema** main 19741fb → **a666738** (ws-a4); `node --test test/release.test.js` 11/11. **ctd-transects** main d080f9b → **b4e4a2a** (ws-a4); no pytest in `.venv` or system python, so the unittest files ran directly: `python3 scripts/test_fetch_citation.py` 6 OK, `python3 scripts/test_resolve_release.py` 20 OK.
- **workflows conflicts:** (1) ws-h2: the WS-H plan file — H3's `## H3 DONE` and H2's `## Measured` both appended; both kept, H3's first. (2) ws-b2: `RELEASES.md` — H2's three sections then B2's one, all at the end of `# Unreleased` before `# v2026.08.25` (H3's entry is a `###` inside H1's section, no conflict); `metadata/field_dictionary.csv` — H2 had appended `dwc_term` to all 57 rows (first 9 fields = base, verified), B2 had added 4 rows (`source_uuid`, `station_uuid`, `station_uuid_method`, `cruise_key_datasets`) and edited 2 (`cruise_uuid` notes, `cruise_key_method` description) → resolved as B2's 61 rows + H2's column, **and B2's six rows were added to `libs/build_field_dictionary.R`** (the CSV is generated; CLAUDE.md says "add a row there, not to the CSV") — `Rscript libs/build_field_dictionary.R` reproduces the resolved CSV exactly (61 rows, 12 DwC terms). `CLAUDE.md`, `metadata/swfsc/ichthyo/questions.csv`, `release_database.qmd` auto-merged.
- **Checks (installed 3.32.0 / 1.17.0):** 17 `questions.csv` registries, 205 rows, ids + labels unique (ichthyo Q13 open / Q14 proposed / Q15 open); `Rscript scripts/declare_measurement_vocab.R` → "measurement_type fields unchanged" (no `CALCOFI4DB_DIR` needed now); `CALCOFI_SKIP_LINK_CHECK=1 Rscript scripts/build_workflows_index.R` exit 0 — 4 ok / 14 exempt / 2 drift, 53 pages — then `git checkout _output/` (untouched); `knitr::purl()` + `parse()` OK for release_database (325 expr), test_release (61), publish_to-erddap (100; prints a pre-existing `object 'DEPLOY' not found` from its `#| eval: !expr DEPLOY` chunk option — main has the same), ingest_swfsc_ichthyo (182); `read_measurement_type()` strict 200 × 20, six `r_*` rows `is_canonical = FALSE`, 115 P01 / 174 P06; CLAUDE.md 32 headings, none duplicated. Every merged tip is an ancestor of its main (workflows: of 4d4f932).
- **Left:** docs `test.txt`/`test.R` + the origin/main catch-up; the DEPLOY purl message (cosmetic, pre-existing); everything wave 1 left (E Ph3 wiring, `publish_to-netcdf.qmd` obs reads before the objects drop); a fresh `pip install -e .` is not needed (path install).

## WS-F Section 3 Measured (Sonnet, 2026-09-04) — staging v2026.09.04, `release_database` + `test_release` both green
- `release_database` completed end-to-end for the first time on attempt 4 (2h28m58s, all 57 chunks), after four attempts each stopping on a genuine gate/bug: dungeness-crab's `source_uuid` NULL reconciler (fixed upstream, calcofi4db 4.0.1) → farallon `cruise_coverage` (0 → 2 orphan cruises allowed by name, `3cf4d11`, Q11) → `DROP VIEW IF EXISTS cruise` erroring on a Table (`c77de85`) → `cruise_key_integrity`'s undeclared `key_null_max` baselines for 8 datasets (`9c38799`, measured: phyllosoma 44, phytoplankton 168, euphausiids 420, picoplankton 7216, zoodb 111, zooscan 421, farallon 1228, cufes 5053; crab lowered 1639→97). Catalog: 18→23 tables (+climatology, +obs_bio, +obs_env, +sample_root, +sample_spatial; `obs` kept deprecated/replaced_by; no removals); cruise 691→842 (+151 derived); `source_uuid` ichthyo 100%, `station_uuid` bottle 79.0%/CTD 81.0%/PIC 65.3%/crab 81.0%.
- **Incident: `test_release.qmd`'s `save_results` chunk hardcoded the GCS upload path to the real prefix** (`gcs_release <- glue("ducklake/releases/{release_version}")`, ignoring `CALCOFI_RELEASE_PREFIX` that every other path in the file reads) — a staging test run wrote a real, phantom `gs://calcofi-db/ducklake/releases/v2026.09.04/test_results.json`. `latest.txt` on the real prefix was never at risk (the `promote` chunk's `stop()` on failed queries runs before `promote_release()` unconditionally). Found and reported by the agent; Ben fixed both the stray object (backed up, deleted) and the code (`ad99de5`, `release_prefix` var) within ~5 minutes.
- `test_release` first run: 58 pass / **3 fail** (farallon "unreachable by worms_id" ×3 forms, n=123 each — some ERDDAP Aves resolve only via ITIS, MEGU/SBIG → `itis:1192602`) / 4 skip — correctly refused to promote. Ben fixed the contract to `worms_id OR itis_id` (`19a30fa`). Rerun (`test_release` only, `shortcut = TRUE`, `release_database` not re-executed, 8m8.8s): **61 pass / 0 fail / 4 skip**. `test_results.json` correctly landed at `gs://calcofi-db/ducklake-staging/releases/v2026.09.04/test_results.json`; staging `latest.txt` promoted to `v2026.09.04`. Re-verified real prefix untouched: 0 objects under `gs://calcofi-db/ducklake/releases/v2026.09.04`, real `latest.txt` still `v2026.08.25`.
- Commits: `942cc51` (Section 1, 13 changed ingests, calcofi4db 4.0.1), `f16907b`/`8b1d71e` (Section 3 failed-attempt meta bookkeeping), `f03180a` (staging v2026.09.04 release_database success + the stray-bucket bug report), `8720586` (test_release green). Section 4 (the real release) NOT started — needs Ben's explicit go.

### WS-F Section 5 — Explorer prefix flip (integrator, 2026-09-04 22:40)

Measured: calcofi.io/explore read the explore-dev cut of v2026.08.25 while app.calcofi.io/hex already showed
v2026.09.04 (Ben). Flipped both Pages builds (`/explore/` and the `/explore/v2/` preview) to
`VITE_DATA_URL=https://storage.googleapis.com/calcofi-db/` + `VITE_RELEASE_PREFIX=ducklake/releases`
(explore f71006d). Contract check before the push: every table the Explorer reads is a strict superset of
the dev cut (obs_bio/obs_env +sample_key, measurement_prec, hex_id; dataset +6 attribution columns; cruise
+2; measurement_type +5; the rest identical). New `scripts/smoke_release.mjs` (headless, no window) drove the
local build and then the live site: page names v2026.09.04, zero console/page errors, every calcofi-db
object 200 (latest.txt, catalog, coverage, grid, spatial_layers, obs_bio, taxon, dataset, measurement_type,
sample_spatial); the one "failed" request is DuckDB-WASM aborting its unused `duckdb-eh` bundle probe.
The welcome card (agree-to-cite) shows once per browser (`explore_welcome` in localStorage); `?tour=on`
forces it, `?tour=off` never shows it — verified on the local build with a screenshot.

## WS-F Sections 4/4b Measured (Sonnet, 2026-09-04) — REAL release v2026.09.04 cut, tagged, released, DOI minted
Env vars confirmed empty for every real-prefix R call (`CALCOFI_RELEASE_PREFIX`/`CALCOFI_RELEASE_LAYOUT`
unset); `shortcut = TRUE` throughout so no ingest re-ran (Section 2's ichthyo reference-shard gate is what
makes that honest). Per-target wall times: `release_database` **1h30m18s** (vs 2h29m under staging — most
objects already content-addressed from the staging run, so `freeze_plan()` found `copy`/`exists` not
`upload`), `test_release` **8m29s** (61 pass / 0 fail / 4 skip, same as staging), `deploy_consumers`
**15m14s** (prep_db.R on the server for db-viz-hex, no error), `publish_to_netcdf` **1h45m26s** (3.6 GB
`calcofi_ctd-cast_full.nc` alone took ~40 min to upload), `publish_to_erddap` **5m33s**.

- **`publish_to_netcdf` failed first** (`connect`, 14.7s): `cc_release_parquet()` in `libs/publish_netcdf.R`
  refused `obs` as "partitioned (15 objects)" — `obs`'s deprecated compat object is now genuinely
  partitioned by `dataset_key` (H1), but the release also ships a single-file twin
  (`releases/{v}/parquet/obs.parquet`) that `calcofi4r::cc_release_sources()` reports as `src$single_file`.
  Ben fixed in `12ca22f`: the function now returns the twin when present. Verified from the checkout before
  rerun (bottle 11,135,600 / ctd-cast 13,295,014 / dic 3,708 via grouped counts over https). Rerun succeeded.
- **`publish_to_erddap` failed twice** (`deploy-run`, ~20s each): `erddap_sync_parquet()` in
  `libs/erddap_deploy.R` built its copy manifest from `lapply(cat_$tables, ...)` over **every** object per
  table; `obs`'s new single-file twin has no `partition_by`/`partition_value`, so `sprintf()` returned
  `character(0)` for it and `vapply(..., "")` aborted ("values must be length 1, but FUN(X[[16]]) result is
  length 0"). Ben fixed in `fe5e2ef`: a new `erddap_sync_manifest()` pure helper builds pairs from the
  **partition objects only** (371 pairs = 372 objects − the twin; dry-run verified against the real
  v2026.09.04 catalog before commit). Rerun succeeded — the "1.–5." deploy-run lines: **1.** synced 371
  parquet files; **2.** built view db (37 views, none empty); **3.** spliced 37 generated + 8
  hand-maintained, added 0 / retiring 0; **4.** flagged for reload; **5.** live verification — every
  datasetID `ok = TRUE` (200/200). `density_per_10m2` (45 occurrences) and effort columns
  (`std_haul_factor`/`effort_class`, 92 occurrences) confirmed present in `datasets_calcofi.xml` for the bio
  datasets — the WS-H gate.
- **Own mistake, self-corrected before commit:** first ran `scripts/build_citation_files.R` with no
  arguments, which silently defaulted to the stale `v2026.09.03-alpha` placeholder (the script's own header
  documents `v2026.09.03 2026-09-03 # before WS-F tags a release`). Re-ran with `v2026.09.04 2026-09-04`
  before staging anything for commit; verified `.zenodo.json`/`CITATION.cff` both show `v2026.09.04`.
- **Caddy redirect reload not completed — flagged, not silently skipped.** `scripts/build_release_redirects.R`
  ran locally and regenerated `server/caddy/releases_redirects.caddy` (214 redirects, 29 versions) in the
  sibling `server` checkout, but completing the reload needs (a) committing+pushing that **different repo**
  (outside this run's authorized scope — only `workflows` main was to be pushed) and (b) `git pull` +
  `docker exec caddy caddy reload` (or restart) on the production host over SSH — confirmed reachable
  (`ssh calcofi`, Caddy runs in a Docker container, config at
  `/ssd/share/github/CalCOFI/server/caddy/Caddyfile`) but a live-server mutation this run wasn't told to
  perform. Needs a human/separate authorized step.
- **New, unresolved finding: `verify_release_objects.R v2026.09.04` reports ~40 size/sha256 mismatches**
  between the uploaded GCS object and `catalog.json`'s recorded hash — every `obs_mets_full` cruise
  partition (dozens) plus 2 `obs_ctd_full` partitions (`cruise_key=2026-04-3322`, `cruise_key=2026-07-3322`).
  The script exits 0 (non-fatal) so the release proceeded, but this needs investigation — possibly
  non-deterministic parquet writing for these two supplemental tables (see CLAUDE.md's byte-determinism
  requirements: unique `ORDER BY` + `threads = 1`) causing the local file to differ between when the catalog
  hash was computed and when the object was actually uploaded.
- Real `latest.txt` promoted to **v2026.09.04** (verified via `gcloud storage cat`). Tag `v2026.09.04` pushed;
  GitHub release: https://github.com/CalCOFI/workflows/releases/tag/v2026.09.04. Zenodo DOI minted within
  the 10-minute poll window: **10.5281/zenodo.22310858** (concept `10.5281/zenodo.22281994`, created
  2026-09-04T20:45:33Z) — landed in `catalog.json`, `versions.json` and the "How to cite" block via a second
  `publish_release_notes.R v2026.09.04` run.
- Commits (workflows main, all pushed): `968dc83` ("[pipeline] release v2026.09.04" — RELEASES.md's renamed
  `# v2026.09.04` section, 5 `_output/*.html`, `_targets/meta/meta`, `data/releases/v2026.09.04/` sidecars +
  `_release_stamp.json`, `.zenodo.json`, `CITATION.cff`), `02e96f7` ("release notes: v2026.09.04 DOI
  10.5281/zenodo.22310858" — catalog.json + RELEASE_NOTES.md re-published with the DOI). Fix commits on main
  by Ben: `12ca22f` (netcdf obs-twin), `fe5e2ef` (erddap partition-only manifest). Section 4b step-by-step
  otherwise exactly as briefed. **Section 5 (Explorer flip, calcofi4r/py tags, db-schema, docs, Shiny
  deploys) was dispatched separately (see the entry above) and NOT started by this agent.**

### The 173 object mismatches — cause, repair, fixes (integrator, 2026-09-04 23:15)

Measured: `verify_release_objects.R v2026.09.04 --max-mb 200` → 372 checked, 173 objects with size AND
sha256 mismatches: all 133 `obs_ctd_full` and 38 `obs_mets_full` cruise partitions plus `obs`'s
`calcofi_ctd-cast` and `calcofi_mets` partitions. Every local frozen file matches the catalog. Chain:
1. Writes are NOT byte-deterministic for those tables (row-identical, byte-different between the
   staging and real runs — `release_sort_keys()` orders them by cruise_key + core sort + obs_id, so a
   non-unique obs_id in the supplementals, or NULL ties, is the likely reason; OPEN).
2. The staging run (canonical layout, default `CALCOFI_TABLES_PREFIX=ducklake/tables`) had no
   previous canonical catalog under its prefix, so every object was `upload` — straight over the
   promoted v2026.08.25 canonical objects with the same content_hash (e.g. obs_ctd_full 1993-08-32NM:
   7,360,117 → 7,359,787 bytes at 15:27Z).
3. The real run (compat layout, prev = v2026.08.25's catalog) planned `copy` for those objects from
   the (now clobbered) canonical paths, while `build_release_catalog()` recorded the LOCAL export's
   bytes/sha256 — a third variant.
Repair: re-uploaded the 173 real-run local files over `releases/v2026.09.04/parquet/…` (the catalog
already describes them; verify re-run pending). The 173 v2026.08.25 canonical objects can be restored
by server-side copy from that release's intact compat copies (dry-run: all 173 compat sizes match its
catalog) — `scratchpad/restore_0825_canonical.sh … --execute`, awaiting Ben's go.
Fixes: calcofi4db 4.0.3 `freeze_plan()` carries the previous release's bytes/sha256 for
copy/exists objects (`bytes_local`/`sha256_local` keep the local values; regression test);
`release_database.qmd` stops a staging run whose tables prefix is the shared store; CLAUDE.md +
release-objects skill say `CALCOFI_TABLES_PREFIX=ducklake-staging/tables` is required.
