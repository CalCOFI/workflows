# WS-M3 — the generator: `/measurements/`, one page per key, JSON-LD, sitemap, search (D2, D6)

**Umbrella:** `.claude/plans/2026-09-10 Measurements catalog — the environment's Species: one page per measurement, the catalog's three indexes on the front door, agent-scaled.md` § D2, D6, § Verification M3. **Spec:** the artifact https://claude.ai/code/artifact/acc4f6c0-814e-46fe-a79a-a804edebe196, sections */measurements/* and *Temperature* (the copy, the rows, the ways in, the notes). **Agent:** `ws-opus-medium`. **Repo:** CalCOFI/CalCOFI.github.io, worktree `~/Github/CalCOFI/.worktrees/CalCOFI.github.io-ws-m3`, branch `ws-m3` (WS-M4 continues on it). **Wave 2 · ≈ 1 day.** Needs `MEASUREMENTS_RELEASE_URL` (the integrator sets it) or a local `_data/measurements.json`.

## Read first
- The umbrella's § D2, D6, Appendix A; the mock's Temperature section (every block, in order) and its *Spec* rules.
- `_plugins/species.rb` end to end (the record class, `page_url`, `ways()`, `erddap_way()` + the `erddap_taxon_key.json` probe, `jsonld()`, `generate()`, `json_page()`, `sitemap()`, the D-2 no-record path), `_layouts/species_index.html`, `_layouts/species.html`, `_includes/species_rows.html`, `_includes/species_ways.html`, `_layouts/dataset.html` (§ Coverage → Variables, and how taxa link their pages), `scripts/fetch_release.sh` (how `taxa.json` is resolved: promoted → `TAXA_RELEASE_URL` → nothing with a NOTE), `scripts/check_jsonld.py`, README § The species catalog.

## You own
`scripts/fetch_release.sh` (the `measurements.json` block + `MEASUREMENTS_RELEASE_URL` in `pages.yml`/`refresh.yml`/`pr.yml`/`check-brand.yml`), `_plugins/measurements.rb` (new), `_layouts/measurements_index.html`, `_layouts/measurement.html`, `_includes/measurement_rows.html`, `_includes/measurement_ways.html`, `_includes/measurement_related.html`, `_layouts/dataset.html` (the variables list + a folded *full resolution only* list), `scripts/check_jsonld.py` (the `DefinedTerm` rule), `_data/erddap_measurement_type.json` (the probe, written by `fetch_release.sh`), README § The measurements catalog.

## Do
1. **`fetch_release.sh`**: resolve `measurements.json` exactly as `taxa.json`; probe once per environment ERDDAP table whether it carries a `measurement_type` column (`…/tabledap/{id}.json?measurement_type&distinct()` with a small limit, or the info page) into `_data/erddap_measurement_type.json`.
2. **`_plugins/measurements.rb`** — the species generator's structure: `site.data.measurements` (counts, datasets, the inline JSON for the index — keys, labels, category, units, spans and totals per series, P01; ≈ 60 KB), `/measurements/` (layout `measurements_index`), `/measurements/{key}/` (layout `measurement`, `page.m` = the entry verbatim + derived rows), `/measurements/{key}.json`, `/measurements/sitemap.xml`, `/measurements/search.json` (key, label, units, category, datasets, P01, series names). Without a record: nothing generated, one NOTE, and `site.data.measurements` nil so WS-M0's doors fall back.
3. **The page** (the mock's blocks in order): crumb · eyebrow (Measurement · category · in situ/underway · units) · h1 label · one-line description · ids (P01 and P06 linked to vocab.nerc.ac.uk, key, series) · stats (values · casts · years · datasets · depth · seasons) · *Measured in* rows (dataset dot + name linking its page · values · casts · years · depth · cells · cruises; the l2 line: series, source column, flag column, the quiet pills from `flags[]`) · the strip `<svg>` and the three figure hosts for WS-M4 (`#mm-strip`, `#mm-depth`, `#mm-months`) with their inline JSON · *Range & quality* (bounds declared per series, observed min/median/max and 5th–95th, flag counts by code with the `measurement_qual.csv` labels where the site has them, `qual_ok`, the baseline line when `climatology` is true) · *Related measurements* (from `related[]`, each linking its page, with the `why` sentence) · *Ways in* (Explorer `?var={key}` — only for keys the Explorer lists, i.e. those in `coverage.json` — the anomaly section link for keys with a climatology, db-query with the prefilled `__TBL:obs_env__` query over the key's series, one ERDDAP row per series whose table passed the probe, the Parquet note through the catalog, R and Python snippets) · *This page as data* · *Cite*. A URL is a full-width single mono line (WS-M4 elides it).
4. **JSON-LD**: `DefinedTerm` (`name` = label, `termCode` = P01 id when present, `inDefinedTermSet` = the NERC P01 collection URL, `url` = the page) plus one `PropertyValue` per series (`propertyID` = P01 URL, `unitText`, `unitCode` from P06 when present, `description`); the index page a `DefinedTermSet`/`ItemList`. Extend `check_jsonld.py` with the rule and the sitemap.
5. **Dataset pages**: each variable in § Coverage → Variables links its measurement page; a folded *full resolution only · n* list from `datasets[].full_resolution_only[]`.
6. README § The measurements catalog: what generates what, the bridge, the probe, the fallbacks.

## Gates (stop and report)
- A record field the layout needs that Appendix A does not carry (say which; do not invent it); `check_jsonld.py` red; a page that would type a number.

## Hand back
The generated counts (pages, sitemap URLs, search rows); `check_jsonld.py` output; the temperature page's rendered blocks checked against the mock (a list of any divergence and why); the ERDDAP probe result per table; branch + commits; one *Measured* line.
