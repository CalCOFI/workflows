# WS-F3 — the species page: the face row, the sentence, the glance, the ladder, the plate, JSON-LD, the checks (D1–D5, D8, D9)

**Umbrella:** `.claude/plans/2026-09-11 Species faces — a silhouette, a licensed photo, a size on a familiar scale and a sourced sentence for every species page, agent-scaled.md` § D1–D5, D8, D9, § Verification F3. **Spec:** the artifact https://claude.ai/code/artifact/34135a47-7597-41af-ad58-5328bde815dd — *The page head, composed* (both themes), *How big is it*, *The sentence*; the ten cast taxa show every state (no size, no photo, a stand-in silhouette, a genus). **Agent:** `ws-opus-medium`. **Repo:** CalCOFI/CalCOFI.github.io in a worktree `~/Github/CalCOFI/.worktrees/CalCOFI.github.io-ws-f3`, branch `ws-f3`. **Wave 1 · ≈ 1.5 days.**

## Read first
- The umbrella's decisions D1–D5, D8, D9 and Appendix A (the JSON you read); `.claude/plans/2026-09-11 species-faces-probe/taxa_media.sample.json` — copy it to `_data/taxa_media.json` for local builds (URLs instead of cached files; the real one arrives from WS-F2a with `cached` paths and `silhouette.svg_inner`; handle both: prefer `cached`, fall back to `url`; prefer `svg_inner`, fall back to fetching nothing and drawing no silhouette).
- The mockup's template `species_faces.template.html` in the same folder: the CSS for the fade (`.ph` mask), the glance (`.glance`), the ladder (`.ladder`), the beside figure (`.beside`) and the JS `sizeGlance`, `sentenceHtml`, `ladder`, `beside`, `plate`, `refGlyph`, `assignRows`. Port them into the site's idiom (brand tokens, `--space`, `.sp-*` names); do not paste the mockup's page chrome.
- The site: `_plugins/species.rb` (how `page.*` is built from `taxa.json`), `_layouts/species.html`, `_includes/species_rows.html`, `_includes/species_ways.html`, `assets/species.js` (the strip, the URL elide), `style.css` `.sp-*` block, `scripts/check_jsonld.py`, `scripts/check_layout.py`, `_data/shots.yml`, `scripts/shots.sh`, `README.md` § The species catalog; `brand/v2/theme.css` (the tokens; frozen, additive only — site-local tokens go in `style.css`).
- `.claude/plans/2026-09-09 Landing follow-ups …` § D6, D8.4 (what the page holds today and why: one page per taxon_key, the accepted name once).

## You own
`_plugins/species.rb`, `_layouts/species.html`, `_includes/species_face.html` (new), `_includes/species_size.html` (new), `assets/species.js` (your own `// ── faces (WS-F3)` section; WS-F4 appends its own after yours), `style.css` (your own `/* ── faces (WS-F3) */` block), `scripts/check_jsonld.py`, `scripts/check_layout.py`, `_data/shots.yml`, `README.md` § The species catalog → *Faces · the page* subsection.

## Do
1. **`species.rb`** — read `site.data.taxa_media` when present; `page.face = taxa[taxon_key]` (nil-safe; a page without an entry renders exactly as today). Compose server-side: `page.sentence = { wp: lead, wp_about_genus:, wp_title:, wp_url:, wp_revision:, rec: "CalCOFI holds N records of it in n datasets between y0 and y1, as <stages>[, across k taxa under it]." , au: "Named by <author> in <year>[; grows to <len> (<kind>)]." }` — `N` is `n_present` with the word *observations* when the record carries `n_present` (D9), else `n_obs` with *records*; the authority parsed from `taxonomic_authority`/`worms.authority` if the record has it, else omit the clause; `page.glance = { org_m, org_label, ref_key, ref_label, ref_m }` choosing the reference from `_data/size_reference.csv` with the smallest |log10(org/ref)| among quarter, ring, person, ship (hair and mesh only when org < 2 mm); `page.credits` (silhouette · photo · text lines per D8's credit format). Numbers only from the record and the sidecar.
2. **`_includes/species_face.html`** — under the ids row: the face row (silhouette inline SVG with `fill: currentColor`, `role="img"`, `aria-label`; the "drawn from X, n ranks up" note when `taxon_shown` ≠ the page's taxon; the photo `<img>` in the 3 : 2 masked frame with `width`/`height`, `alt = shows`, `loading="lazy"`, `object-position` from `focal`; the NC chip when the licence is NC; the glance's two bars or "not on record"); then the sentence with `class="s-wp | s-rec | s-au"` spans and a one-line legend; then the credit line. Absent slots leave no gap.
3. **`_includes/species_size.html`** — a *How big* section after *Observed in*: the ladder (references from `size_reference.csv` inlined as JSON with the taxon's marks: adult max; egg, hatching, flexion, transformation from `early[]`), the beside figure, the plate (`plate.cached`/`url`, credit line, the `early[]` list with sources), all drawn by `assets/species.js` from one inline `<script type="application/json" id="sp-size">`; the "not on record" and "no plate" states as in the mockup; `--plate-filter` as a site-local token for dark.
4. **`style.css`** — additive `.sp-face`, `.sp-sil`, `.sp-photo` (the radial mask on the frame, not the image), `.sp-glance`, `.sp-sent`, `.sp-ladder`, `.sp-beside`, `.sp-plate`, all on brand tokens; the page at 375 stacks the face row.
5. **JSON-LD** — when a photo exists, `image: { "@type": "ImageObject", contentUrl, license, creditText, acquireLicensePage }` (D8); `check_jsonld.py` asserts it on every page with a photo and nothing new on pages without.
6. **`check_layout.py`** — at 1470 and 375, both themes, on `/species/worms-217452/` (five ladder marks, a plate), `/species/worms-148985/` (no size, no plate, no gap), `/species/itis-1255050/` (an *Ardenna* stand-in flagged) and one page with no `taxa_media` entry (byte-identical to today except the sentence's record part): the face row present where expected, no horizontal scroll, no two ladder labels overlapping (bounding boxes via the DOM), the stat word records/observations per D9.
7. **`_data/shots.yml`** — reshoot `species_page` (and add `species_page_dark` if the file has per-theme shots).
8. **README** § The species catalog → *Faces · the page*: what the row shows, where each part comes from, the credit line, the D9 wording rule.

## Gates (stop and report)
- The layout probe red; a page without a sidecar entry differs from today's build by more than the sentence's record part.
- A number you would have to type (a reference length, a licence, a credit): stop, it belongs in the sidecar or the csv.
- `taxa.json` lacks the authority string and you want to fetch it: do not; omit the clause and say so.

## Hand back
Branch + commits; screenshots of the four pages at 1470/375 both themes; the probe and `check_jsonld.py` output; one JSON-LD sample; the README text; one *Measured* line for the umbrella.
