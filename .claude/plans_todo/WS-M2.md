# WS-M2 — the record: `build_measurements_catalog()` → `measurements.json` (D4)

**Umbrella:** `.claude/plans/2026-09-10 Measurements catalog — the environment's Species: one page per measurement, the catalog's three indexes on the front door, agent-scaled.md` § D2, D4, Appendix A (the contract — implement it, do not redesign it). **Agent:** `ws-opus-medium`. **Repos:** CalCOFI/calcofi4db (worktree `../.worktrees/calcofi4db-ws-m2`, branch `ws-m2`) and CalCOFI/workflows (`~/Github/CalCOFI/.worktrees/workflows-ws-m2`, branch `ws-m2`). **Wave 1 · ≈ 1 day.**

## Read first
- The umbrella's § Context (the temperature numbers you must reproduce), § D2, § D4, Appendix A, § Verification M2.
- `calcofi4db/R/catalog_taxa.R` end to end (`build_taxa_catalog()`, `write_`, `validate_`, `check_`, `assert_`, `taxa_source_flags()`, the six grouped queries, the split-index pattern, `.taxa_val/.taxa_chr`), `inst/schema/taxa.schema.json`, `tests/testthat/test-catalog_taxa.R` (the synthetic DuckDB fixture); `R/coverage.R` `build_coverage()` (`variables[]` — add `label`); `R/registry.R` (`read_measurement_type()`; WS-M1's `read_variable()` if merged, else read `variable.csv` with `readr` and the same rule); `release_database.qmd` step 3b′ (where `taxa.json` is written) and `RELEASE_REQUIRED_OBJECTS`; `test_release.qmd` `taxa_catalog` gate; the `core-model` and `release-run` skills.

## You own
`calcofi4db/R/catalog_measurements.R` (new), `inst/schema/measurements.schema.json` (new), its tests, `R/coverage.R` (the one `label` addition), your own `NEWS.md` section (no `DESCRIPTION` bump — the integrator assigns the version), `release_database.qmd` step 3b″, `RELEASE_REQUIRED_OBJECTS`, the `measurements_catalog` gate in `test_release.qmd`, your own `##` in `RELEASES.md # Unreleased`.

## Do
1. **`build_measurements_catalog(con, record, measurement_type, variable = NULL, category, release_version = NULL, release_date = NULL)`** — Appendix A's shape exactly. Grouped queries on `obs_env` (never per key): by `dataset_key × measurement_type` (n, distinct `sample_key`, distinct `root_id`, distinct `grid_key`, distinct `cruise_key`, min/max year, min/max `depth_min_m`/`depth_max_m`, quantiles of `value`, `qual_ok` count), × `year`, × `EXTRACT(month FROM datetime)`, × depth band (`0-10, 10-50, 50-100, 100-200, 200-500, 500-1000, 1000-2000, 2000+` on `depth_min_m`), × `measurement_qual`. Keys = `variable ∥ measurement_type` over the series present in `obs_env`; `datasets[]` from the record for names, colours, categories and order; `full_resolution_only[]` = registry rows with `is_canonical == FALSE` (or a `_source_table` in a supplemental) for that dataset that are absent from `obs_env`; `related[]` from the same P01 across keys with `why` per the umbrella's Appendix B categories (`same_bottles` when the other series is a `btl_*`/`_btl` of the CTD dataset; `replicate_vs_mean` on `_rep`; `underway_vs_cast` when the other dataset is `calcofi_mets`; `pre_qc_twin` on `r_`); `flags[]` per series from the registry (`derivation` mentions "Mean of" → `sensor_mean`; `_rep` → `replicate`; `r_` prefix → `reported_pre_qc`; empty `valid_min`/`valid_max` → `no_bound`; observed max > registry max or a value ≥ 99 with no bound → `sentinel_suspected`; no `qual_column` → `no_flag_at_grain`; empty `nerc_p01` → `no_p01`). Deterministic; no network; `slug = key`.
2. `write_measurements_catalog()`, `validate_measurements_catalog()` (jsonvalidate against the schema), `check_measurements_catalog()` (the arithmetic: sum of series `n_values` == `obs_env` rows; every `years{}` sums to its series; every `dataset_key` in the record; every `measurement_type` in the registry), `measurement_series_flags()` (the enum). Tests on a synthetic DuckDB fixture (≈ 6 series in 3 datasets): a unified key with two series, a single-series key, a non-canonical series → `full_resolution_only`, a `no_p01` series, a flag vocabulary, the arithmetic check, a `variable.csv` label vs the fallback with `no_label`.
3. `build_coverage()`: `variables[]` gains `label` from `variable.csv` when a row exists (the Explorer's `setUnified()` hook); nothing else changes.
4. `release_database.qmd` **3b″** after 3b′: build, validate, write `measurements.json` beside `taxa.json`; add to `RELEASE_REQUIRED_OBJECTS`; `test_release.qmd` gate `measurements_catalog` (schema valid; counts; arithmetic; the temperature entry's `totals.n_values` == the two series' sum) feeding the promote gate; `RELEASES.md # Unreleased` entry with a **Consumers:** line (the landing reads it; nothing else yet). Purl + parse both notebooks; run neither.
5. **The provisional record**: with `devtools::load_all()`, build from the promoted v2026.09.06 (`calcofi4r::cc_get_db()` for `con`; `datasets.json` from GCS for `record`; the registries from the workflows worktree) and validate. Report the numbers of § Verification M2. Leave the file in the worktree (`measurements.v2026.09.06.json`) for the integrator to upload — do not upload.

## Gates (stop and report)
- The arithmetic check fails on v2026.09.06 (a series counted twice or a null-year gap larger than the umbrella states); the temperature numbers do not reproduce; a shape decision Appendix A does not settle; `devtools::test()` red.

## Hand back
Functions and files; test count + result (full suite too); the provisional record's counts and the temperature entry's key numbers; the schema; the two notebook diffs (chunk names); the `RELEASES.md` text; branches + commits (both repos); one *Measured* line.
