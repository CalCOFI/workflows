# 2026-09-08_CalCOFI.io_update_polished.pptx — change log

Same 24 slides, same order, same titles, numbers, URLs and speaker notes as `2026-09-08_CalCOFI.io_update.pptx`. Checked mechanically: per slide, the word multiset, every number, every URL and the notes (verbatim) are identical between the two decks; the only textual change is that the script's literal "•  " prefixes became real PowerPoint bullets. Everything is drawn with brand v2 tokens only (`theme.css`): navy, blue, yellow, sand, stone, `--muted #66686A`, `--warn #8A6500`, and the dark ground/panel/border tones; Source Sans 3 and Teko (uppercase display via `cap="all"` — the title text itself is unchanged); the 54-glyph sprite, the two lockups, and the sea-floor profile traced from the hero screenshot.

**Deck-wide.** Slim white header with the horizontal lockup far left and a hairline; eyebrow + Teko title with a brand glyph beside it; sand band holding the screenshots as rounded (8 px) borderless cards with a soft navy shadow; footer strings kept as-is. Tables restyled (navy header, sand zebra, no vertical rules). Plain 0.5 s fade between slides; one automatic 0.3 s fade build per slide (the argument column, table or callout appears right after the slide does — no extra clicks). Notes copied word for word.

| # | What changed |
|---|---|
| 1 | Dark opener: two-line Teko title, the four facts as one navy numbers band (Teko numerals, small-caps labels, hairline dividers), the ship glyph and the GEBCO sea-floor silhouette low on the slide. |
| 2 | Five sand cards hung off a horizontal "stations" line (white station discs), each headed by a glyph (home, stations, data, share, code); closing sentence kept. |
| 3 | Hero screenshot as a large card (8.3 in) on the band; bullets at 15 pt. |
| 4 | Bento screenshot takes the left two-thirds; bullets, the apps-strip card and its caption on the right third. |
| 5 | Specimen light and dark side by side; below, a tidy row of lockup sheet · buttons + chips (with the original caption) · icon sheet; bullets on the right. Not added: extra "tiny labels" — they would be new words. |
| 6 | Default-view card on the band; bullets at 17 pt. |
| 7 | Yellow numbered callouts at the original fractional positions; the legend descends like a CTD cast — a wire with faint depth-ruler ticks and the six numbered "bottles" aligned to the callouts' numbers. |
| 8 | Three equal columns (layers · pane controls · help) with captions beneath, bullets full width below. |
| 9 | Six tiles: lens glyph + Teko name in navy, thumbnail card, one line of type. |
| 10 | Navy band (the talk's hero): the contours screenshot as large as the band allows, white bullets, the fit line as the slide's one yellow chip. **Resolved:** the script had stacked two screenshots here; the visible one (explore_contour_2026-09-07, 3,988 sites / 1.45 °C) contradicted the caption and notes, so the deck now shows only the shot the caption describes (213 stations → 56,591 cells, 0.79 °C). |
| 11 | Explorer panel larger, 147 ms in blue at 66 pt; Shiny panel smaller, 11,353 ms in the kit's AA gold (`#8A6500` — `#C69214` is 2.8:1 on white). The two uptime sparklines were recoloured blue / gold (shape untouched; they are graphs, not UI). Cost bullet verbatim. |
| 12 | Four shots in a row with captions, the two sentences, then the table: navy header, sand zebra, Missing as a Teko numeral with "of 16" quiet, dataset keys as quiet chips (highlighted runs, commas kept). |
| 13 | Annotated dialog as the hero; the three header crops scaled so the speech bubble sits on one dashed column; "Register a product" in a white callout on the sand band with its sentence beside it. |
| 14–15 | Screenshot card + bullets at 17 pt. |
| 16 | Brand table (Portal column bold) + bullets. |
| 17 | Both figures as cards on the band with their labels above; bullets below. |
| 18 | Zenodo and RELEASES cards; "How to cite" as a sand callout. |
| 19 | Two sand code panels headed by the code glyph; code in the kit's `--mono` stack (Menlo; the original used Source Code Pro, which is not in the kit). |
| 20 | The ingest loop descends like a cast: wire, four bottles, four white stage cards with the push/pull · build · generate lines between them. |
| 21 | Docs index, doors table and system figure as cards; bullets at 12 pt. The DMP-phase "cast" was not built — the five parts live inside one bullet and there is no room for a second copy without dropping words. |
| 22 | Brand table, Owner column bold, plain and legible. |
| 23 | Dark closer: Teko statement, the cast of characters (phytoplankton → whale, small to large) above the sea-floor silhouette, ship on the surface. |
| 24 | Two-column URL list (labels right-aligned, URLs blue). |

**Revision 2 (after the first PowerPoint open)**

- Repair prompt: slide 12 carried two shapes with the same id (pptxgenjs gave the table the id of a picture); every slide's shape ids are now renumbered uniquely, and the package is written without directory entries. A four-slide test opened cleanly in PowerPoint on the Mac.
- Oversized title: PowerPoint had no usable Teko and substituted a wide font. Teko (Regular, Bold) and Source Sans 3 (Regular, Bold, Italic, Bold Italic) are now embedded in the file (OFL, `ppt/fonts/*.fntdata`), so the deck carries its own type. The opener and closer were also laid out so that even a fallback font wraps cleanly (taller title boxes, numerals at 32 pt).

**Could not resolve / please note**

- The 18 pt body / 14 pt caption floor could not be met everywhere without cutting words: dense slides run bullets at 11.5–13.5 pt (5, 7, 8, 11, 12, 13, 20, 21), captions at 11.5–13 pt, eyebrow and footer chrome at 12 pt, the attribution and asks tables at 10–12.5 pt. Every pair still passes 4.5:1.
- No section-divider slides exist in the 24, so the divider motifs (silhouette, cast strip) went to the opener and closer, and the "stations line" to slide 2.
- Menlo (code on slide 19) is Apple's and cannot be embedded; it is on every Mac. PowerPoint renders the Teko titles in caps via a text attribute, so copy-paste still yields the original case.
- Animations are hand-written PowerPoint-style timing nodes (validated, and the test deck opened without complaint); flick through once to confirm the fades play as "after previous".
