# CalCOFI Workflows — Jekyll site at https://calcofi.io/workflows/
#
# Built by .github/workflows/jekyll-gh-pages.yml from this _output/ directory.
# The landing page (index.html) is driven by _data/workflows.yml, which is
# generated from each notebook's `calcofi:` YAML front matter by
# scripts/build_workflows_index.R (run it after adding/retitling notebooks).

title:       "CalCOFI Workflows"
description: "Reproducible Quarto / R notebooks that ingest, release, and publish the CalCOFI integrated database."
url:         "https://calcofi.io"
baseurl:     "/workflows"
markdown:    kramdown

exclude:
  - README.md
  - LICENSE
